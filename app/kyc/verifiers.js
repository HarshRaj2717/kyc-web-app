/**
 * @todo implement whatsapp verification
 * @returns boolean (true == verified, false == verification failed)
 */
export function whatsappVerifier() {
  return true;
}

/**
 * @todo implement email verification
 * @returns boolean (true == verified, false == verification failed)
 */
export function mailVerifier() {
  return true;
}

/**
 * @todo implement referral verification
 * @returns boolean (true == verified, false == verification failed)
 */
export function referralVerifier() {
  return true;
}
